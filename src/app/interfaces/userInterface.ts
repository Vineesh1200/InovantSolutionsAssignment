export interface LoginInterface {
    email: string;
    password: number;
    device_type: string;
    device_token: string;
    device_model: string;
    app_version: string;
    os_version: string;
}